# RISKCORE Backend Dependencies

# === Web Framework ===
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9  # For file uploads

# === Database (ON-PREMISES - psycopg2) ===
psycopg2-binary>=2.9.9  # PostgreSQL driver for on-premises deployment
# Note: Use psycopg2 (not -binary) in production for better performance

# === HTTP Client ===
httpx>=0.27.0
requests>=2.31.0

# === Data Validation ===
pydantic>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.1.0

# === File Processing (Week 2) ===
pandas>=2.1.0
openpyxl>=3.1.0  # Excel files
python-dateutil>=2.8.0

# === FIX Protocol (Week 2) ===
simplefix>=1.0.16

# === Risk Libraries (Week 3) ===
# riskfolio-lib>=6.0.0  # Enable when needed
# financepy>=0.350     # Enable when needed

# === Utilities ===
python-dotenv>=1.0.0
ratelimit>=2.2.1

# === Testing ===
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# === Legacy (research scraper - can remove later) ===
beautifulsoup4>=4.12.0
lxml>=5.0.0
trafilatura>=1.6.0
